# Environment name: dev, hml or prd
VITE_APP_NAME=Gestor de Compras Web
VITE_APP_VERSION=0.1.0
VITE_APP_ENV=dev

# Optional absolute backend URL. Keep empty for same-origin /api requests through
# the Vite proxy, including access from another computer on the local network.
VITE_API_BASE_URL=
